{
  "name": "nib",
  "version": "0.0.1",
  "private": true,
  "description": "Learning-first Claude Code clone — built to understand, not to compete.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "typecheck": "tsc -b",
    "nib": "bun run packages/cli/bin/nib"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "latest"
  }
}
